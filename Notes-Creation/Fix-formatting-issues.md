SOURCE_FOLDER="Notes/Week-1"

Review every Markdown file in "$SOURCE_FOLDER" and fix formatting issues only, including malformed LaTeX delimiters, headings, lists, tables, links, spacing, indentation, and code fences. Preserve all wording, code, examples, order, and meaning—do not rewrite, summarize, or add content. Apply the fixes directly, re-scan all files, and report the files changed.